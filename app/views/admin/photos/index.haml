%div#content
  
  %h1 Listing photos

  %table
    %tr
      %th{:style => "width:150px; text-align:left"} Title

    - for photo in @photos
      %tr
        %td= h photo.caption
        %td= link_to 'Show', [:admin, photo]
        %td= link_to 'Edit', edit_admin_photo_path(photo)
        %td= link_to 'Destroy', admin_photo_path(photo), :confirm => 'Are you sure?', :method => :delete
  %p= link_to 'New Photo', new_admin_photo_path